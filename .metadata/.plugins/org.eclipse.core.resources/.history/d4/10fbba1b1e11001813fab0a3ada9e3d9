def mfi(arr):
    dic = {}
    
    for x in arr:
        if x in dic:
            dic[x] += 1
    
    k = list(dic.keys())
    v = list(dic.values())
    
    return dic


# test
arr = [1,1,2,3,4,1,2,6]
print(mfi(arr))